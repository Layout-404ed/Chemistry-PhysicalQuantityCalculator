{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with Homebrew clang + CodeLLDB",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build",
      "stopOnEntry": false
    }
  ]
}
